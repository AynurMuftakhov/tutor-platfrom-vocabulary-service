spring:
  datasource:
    url: jdbc:h2:mem:vocabularytest;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  ai:
    openai:
      api-key: dummy
vocabulary-service:
  base-url: http://localhost:8084
elevenlabs:
  api-key: dummy
  voice-id: dummy